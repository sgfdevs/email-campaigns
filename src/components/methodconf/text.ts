import { colors } from './colors';

export const text = {
    fontSize: '16px',
    color: colors.text,
};

export const heading = {
    color: colors.text,
};

export const link = {
    color: colors.red,
};
